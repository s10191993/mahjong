# 常駐網頁版部署指南

目標：讓麻將有一個**固定網址、隨時可玩**，你的電腦關機也沒關係。

程式已經準備好了（`requirements.txt`、`render.yaml`、`/healthz` 都有），
你只需要做兩件事：**把程式放上 GitHub** → **在 Render 開一個免費服務**。

> 註冊帳號、輸入密碼這種事需要你本人操作，我沒辦法代做。
> 以下每一步都很短，照著點就好。

---

## 第 1 步：把程式放上 GitHub

### 1-1 註冊 / 登入 GitHub
到 https://github.com 註冊或登入（免費）。

### 1-2 建立一個新的空 repository
- 點右上角 **+** → **New repository**
- Repository name：`mahjong`（或你喜歡的名字）
- **Public 或 Private 都可以**（Render 免費版兩種都支援）
- **不要**勾選 Add README / .gitignore / license（因為本機已經有了）
- 按 **Create repository**

### 1-3 把本機程式推上去
建立完成後 GitHub 會顯示你的 repo 網址，長得像
`https://github.com/你的帳號/mahjong.git`

在**這個資料夾**（`線上麻將/`）開啟終端機，執行（把網址換成你的）：

```bash
git remote add origin https://github.com/你的帳號/mahjong.git
git branch -M main
git push -u origin main
```

第一次推送會要求登入 GitHub，照畫面指示完成即可。

---

## 第 2 步：在 Render 開免費服務

### 2-1 註冊 Render
到 https://render.com → **Get Started** → 選 **Sign in with GitHub**（最快，會自動連結你的 repo）。

### 2-2 建立 Web Service
- 進到 Dashboard → **New +** → **Web Service**
- 選擇剛剛那個 `mahjong` repo → **Connect**
- Render 會自動讀取 `render.yaml`，設定應該會自動帶入。若沒有，手動填：

| 欄位 | 填什麼 |
|------|--------|
| Name | `mahjong`（會變成網址的一部分）|
| Region | Singapore（離台灣最近）|
| Runtime / Language | **Python 3** |
| Build Command | `pip install -r requirements.txt` |
| Start Command | `python server.py` |
| Instance Type | **Free** |

- 按 **Create Web Service**

### 2-3 等待部署
畫面會跑安裝流程，約 2-5 分鐘。看到 **Live** 就好了。

你的固定網址會長這樣：

```
https://mahjong-xxxx.onrender.com
```

**把這個網址存起來，以後就都用它。** 傳給朋友，隨時開隨時玩。

---

## 免費方案要知道的兩件事

1. **閒置會休眠**
   15 分鐘沒人連線，服務會睡著。下次有人開網址時會自動喚醒，
   但**第一個人要等約 30～60 秒**（畫面會轉圈圈）。
   喚醒後就正常了，打牌過程中不會睡著。

   > 小技巧：約好打牌前，先自己開一次網址把它叫醒。

2. **重新部署 / 休眠重啟後，進行中的牌局會消失**
   房間資料存在記憶體裡。若真的被重啟，大家回到大廳重開一局即可
   （程式已經會自動清掉失效房號，不會卡住）。

---

## 之後要改程式怎麼辦

在本機改完，推上去就會自動重新部署：

```bash
git add -A
git commit -m "說明改了什麼"
git push
```

Render 會偵測到並自動更新，約 2-5 分鐘後生效。

---

## 想升級成不休眠（選用）

Render 的 **Starter** 方案約 US$7/月，不休眠、隨時秒開。
在 Render 的服務頁面 → **Settings** → **Instance Type** 改成 Starter 即可。

---

## 其他免費選擇（若不想用 Render）

- **Fly.io**：免費額度較彈性、可設不休眠，但要裝 CLI，設定稍複雜。
- **Railway**：介面友善，但免費額度用完就要付費。
- **Koyeb**：也有免費方案，設定跟 Render 類似。

三者都支援 WebSocket，本專案不用改程式就能部署。
